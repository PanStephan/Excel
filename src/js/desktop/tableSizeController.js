import { selectRole } from '../domHelper';

// const tableBody = selectRole('tableBody');

//     while (tableBody.rows.length <= 20 ) {
//       let tr = document.createElement('tr');

//       for(let i = 0; i < 10; i++) {
//         // let td[i] = document.createElement(`td`);
//         // tr.classList.add("class", "list-item" );
//         tr.innerHTML = `<td></td>`
//       }
//       tableBody.append(tr);
//     }

